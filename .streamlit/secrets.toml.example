# Copie este arquivo como secrets.toml (local) ou cole o conteúdo no
# painel "Secrets" do Streamlit Cloud (Settings → Secrets).
#
# NUNCA commite secrets.toml — ele está no .gitignore.

# Chave da API Anthropic (página Pergunte aos Dados)
anthropic_api_key = "sk-ant-api03-…"

# Senha simples de acesso (opcional — habilitar via app.py com st.text_input)
# dashboard_password = "troque-esta-senha"
